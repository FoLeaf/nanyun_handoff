# 对内硬件复用映射表（不写入对外清单/方案正文）

> 用途：交付与工程实施对照。对外材料统一按会议一修技术主线（ESP32 / STM32 / RT-Thread / RS485-Modbus）表述。  
> 原则：硬件最小改动；对外证据链（名称、参数、验收、案例、编制）必须重构。

## 1. 映射总表

| 物联网基底（对内） | 嵌入式对外表述 | 改动级别 | 交付注意 |
|--------------------|----------------|----------|----------|
| 箱体 420×325×150、快接层叠 | 嵌入式综合实验箱（便携一体化、免跳线） | 命名 + 参数侧重点 | 物理箱体可沿用；参数不写“物联网实验箱” |
| STM32G0 主控位 | STM32 实时控制节点 + **RT-Thread** 任务组织 | 软件栈强化（交付重点） | 出厂/验收需可部署 RT-Thread 示例工程 |
| WiFi/BLE 模组位 | **ESP32** 低功耗无线感知节点 | 话术对齐会议（硬件可复用 ESP32 位） | 对外主参数写 ESP32；验收含 Wi-Fi/BLE 感知上报 |
| RS485/串口资源 | **RS485/Modbus** 工业通信与执行联动 | 参数与实验项目强化 | 验收含 RTU 读写、阈值联动风机/卷帘 |
| 传感/执行器开发包 | 同名开发包，验收点改为环控/联动驱动 | 参数改写 | 绑定猪舍/禽舍测点与执行机构 |
| ZigBee / OpenHarmony 模组 | **不出现在对外主参数** | 物理可保留，对内映射 | 对外清单/方案/PPT 禁止主叙事 |
| AIoT / 鸿蒙云平台表述 | 嵌入式智能综合实训云平台（虚实结合、任务闭环） | 能力重写 | 弱化物联网品牌词；强调班级任务闭环 |
| 课程平台独立条目 | 并入云平台教学管理 + 资源同步交付 | 结构已变 | 不单列“物联网课程平台”大额条目 |
| 装修文化建设条目 | 会议版未单列大额装修；方案可简述环境要求 | 结构已变 | 不把装修计入 69.7230 万设备总额 |
| 发酵 / 泛养殖案例 | 生猪 + 蛋鸡 + **综合实训沙盘** | 场景重构 | 闭环句对齐会议：阈值/回差→风机/卷帘反馈 |

## 2. 实验箱内部位与对外主线对照

| 接口位/模组（对内） | 对外参数主写 | 验收示例 |
|---------------------|--------------|----------|
| STM32 控制位 | STM32 实时控制 + GPIO/ADC/PWM/中断 + RT-Thread | 线程调度、设备驱动、环控任务组织 |
| ESP32 / WiFi-BLE 位 | ESP32 无线养殖感知节点 | 低功耗采集、Wi-Fi/BLE 上报、断线重连 |
| RS485 接口 | RS485/Modbus RTU 工业通信 | 从站地址、寄存器读写、联动命令 |
| 温湿度 / 光照 / 气体等 | 猪舍/禽舍环境测点 | 阈值、回差、历史曲线 |
| 继电器 / 风机 / 卷帘模拟 | 执行联动层 | 手动/自动/远程三模式 + 状态反馈 |
| 中心网关 / 触控终端 | 边缘网关 + 本地可视化 | 协议转换、本地规则、离线安全控制 |
| ZigBee 协调器/节点（若物理在箱） | **对外不写主配置条** | 内部物料可保留，不作为教学主线与验收主项 |
| OpenHarmony 模组（若物理在箱） | **对外不写主配置条** | 内部可留位，不出现在对外能力主线 |

## 3. 软件与资源交付映射

| 旧表述（对内） | 新交付口径 |
|----------------|------------|
| OpenHarmony 例程 / DevEco | 改为 STM32 + RT-Thread、ESP32 示例工程（采集/通信/环控） |
| ZigBee 组网实验为主 | 改为 RS485/Modbus 联调 + ESP32 无线感知 + 边缘网关协议转换 |
| 鸿蒙/AIoT 上云主叙事 | 改为嵌入式智能综合实训云平台：任务闭环、仿真+真机、本地部署 |
| 独立课程平台 + 装修大额 | 资源并入云平台与同步交付；环境要求在方案简述 |

## 4. 验收与话术红线（对内）

1. 对外不得出现“原物联网实验箱更名”“换皮”等表述。  
2. 对外检索应无：`OpenHarmony`、`ZigBee`、`鸿蒙物联网` 作为产品主规格。  
3. 对外应稳定出现：`ESP32`、`STM32`、`RT-Thread`、`RS485`/`Modbus`。  
4. 若客户对照旧物联网方案：用编制（13×4）、沙盘、预算 69.7230 万、双案例、闭环验收五板斧说明实质差异。  
5. 写了 RT-Thread 的条目，交付包须含可运行示例与指导书，避免“参数有、交付无”。

## 5. 数字锁定（与对外一致）

- 13 套实验箱 × 4 人/套 ≈ 52 人；约 120㎡  
- 总额 **697230** 元（69.7230 万元）  
- 品目：实验箱×13、五类开发包×13、云平台×1、生猪×1、蛋鸡×1、沙盘×1  
