# 2026-07-30 工作日志

## 样题 1/3/4 套对齐"领导更正版本"

目标：把 `02_样题/` 下第一套、第三套、第四套对齐到 `领导更正版本/第一套` 的版式。
经 grilling 澄清后的决策：
- 范围 = 仅结构+命名（不改文件内容）。
- 第一套 = 整包替换（直接用领导版覆盖）。
- 第三/四套 = 只重组目录+标准化文件名，各自内容原样保留（不复制第一套内容）。

### 目标目录骨架（leader 版式）
```
<套>/01竞赛要求/
<套>/02竞赛素材/0201竞赛素材/   (原 05_U盘资料)
<套>/02竞赛素材/0202答题卡（需打印）/  (原 04_需填写内容)
<套>/03结果呈现/                (原 03_结果呈现_参考答案)
<套>/04评分标准/                (原 02_评分标准)
```

### 文件名标准化映射（参考答案 / 答题卡）
- 03 结果: `模块C_PCB制程工艺卡（参考答案版）` → `模块C-01PCB制程工艺卡（参考答案）`
- 03 结果: `模块C_缺陷记录表-参考答案` → `模块C-02缺陷记录表（参考答案）`
- 03 结果: `模块D_检测记录表_参考答案` → `模块D检测记录表（参考答案）`
- 答题卡: `学生组_模块D_检测记录表` → `模块D_检测记录表`（去掉"学生组_"前缀）

### 执行结果
- 第一套：已整包替换为领导版（11 文件）。
- 第三套：重组完成（10 文件），保留 模块C.zip（与领导版 模块Cgerber.zip 命名不同，属内容资产未改名）。
- 第四套：重组完成（9 文件）；保留既有缺口：05_U盘资料 缺 模块D.eprj2，且 04评分标准 无评分文件。
- 备份：`05_归档备份/对齐前备份_20260730/{第一套,第三套,第四套}`（各为旧结构，共 30 文件）。

### ⚠️ 重要 gotcha（环境）
本环境 Bash 工具存在"命令二次执行"现象：移动/删除类命令会报 `mv: cannot stat ...`
的伪错误（实际第一次执行已成功），且输出 `Sandbox bypassed (escalation-approved)`。
**务必在移动/删除后用绝对路径 `find` 独立核验最终结构**，不要被 stderr 的 cannot stat 误导。

### 待办 / 缺口（告知用户）
- 第三套、第四套 的 `04评分标准/` 目前为空目录，需后续补充评分标准 xlsx。
- 第四套 缺 `模块D.eprj2`（按"不复制内容"原则保留原样）。
- `领导更正版本/` 文件夹仍保留作参考源，未删除。

## 补充：补评分标准模板 + 补 模块D.eprj2（用户确认"要的"）
- 复制 `第一套/04评分标准/评分标准（第一套）.xlsx` → 第三套、第四套 `04评分标准/`，并按套次重命名为（第三套）/（第四套）作模板。
- 复制 `第一套/02竞赛素材/0201竞赛素材/模块D.eprj2` → `第四套/02竞赛素材/0201竞赛素材/`（占位副本，需用户核对是否匹配第四套实际模块D板）。
- 结果：三套均为 11 文件，04评分标准 不再为空；第四套 0201竞赛素材 补齐 模块D.eprj2。
- ⚠️ 第四套的 模块D.eprj2 是基准套副本，非第四套原生文件，使用前请确认正确性。

## 实现第三/四套评分标准（基于题目与参考答案）
- 用 openpyxl（venv: C:/Users/19y/.workbuddy/binaries/python/envs/default）在已复制的副本上改写。
- 关键结论：第三、四套共享同一块 C 板（80×45mm、2层、12mil最小孔、1:1开窗、缺底层丝印GBO）与同一块 D 板（80×60×1.6mm，M01-M05 与 E01/E02 完全相同；D 板工程母版=第四套，第三套仅做网格轴对调派生）。故三套评分框架（A30/B25/C20/D25、各评分逻辑）完全一致，仅 D 板 V01-V05 缺陷类型不同：
  - 基准套(第一套) V：顶层线路缺口 / 底层线宽变窄(颈缩) / 孔破盘 / 阻焊未开窗或偏移 / 丝印缺失或压焊盘
  - 第三、四套 V：走线颈缩(顶层) / 过孔内径过小(钻孔层) / 漏钻孔(钻孔层) / 无法形成有效阻焊桥(顶层) / 丝印缺失或压焊盘(同)
- 改写点（每套6处）：V01-V04 描述、综合判定致命缺陷引用、说明"第一套"→"第三/四套"。V05 与 C 工艺卡、M/E 均不改。
- 输出：第三套/第四套 `04评分标准/评分标准（第三套/第四套）.xlsx`。
- ⚠️ openpyxl 另存会清掉公式缓存；总分 F=SUM(F2,F4,F11,F20)、G=SUM(G2:G34) 公式串仍在，Excel 打开会自动重算显示 100/0。
- 提取脚本与中间产物在 `.workbuddy/tmp_extract/`（setN_题目/各参考答案/评分标准 txt，dump_xlsx.py, dump_docx.py, build_评分标准.py）。

## 14:00 左右 — 第四套整体更名为第二套
- 操作：`02_样题/第四套` → `第二套`（文件夹重命名）；内部 `评分标准（第四套）.xlsx` → `（第二套）.xlsx`。
- 内容同步："第四套" → "第二套" 在 docx 标题（竞赛样题（第二套））与 xlsx 说明单元格（第二套样题配套）各 1 处，验证 0 残留。
- 编辑通道：editor_sdk MCP（tencent-local-office-edit）对本文档报 "document is not open"，按路由规则回退到 python-docx/openpyxl 直接改写并保存；xlsx 总分公式 =SUM(...) 保留。
- 改名前副本：`02_样题/.workbuddy/tmp_extract/bak_第四套_改名前/`（回退用）；早期对齐前备份仍在 `05_归档备份/对齐前备份_20260730/`。
- 现状：02_样题 下为 第一套 / 第二套 / 第三套 / 领导更正版本（无冲突）。

## 14:13 — 全套样题完整性交叉检查
- 脚本 `.workbuddy/tmp_extract/cross_check.py`；报告存 `06_检查报告/样题完整性交叉检查_20260730.md`。
- 结论：四套（第一/二/三/领导更正版）目录骨架+必备文件齐全且非空 → 结构完整。
- 内部标注：第二/三套 docx标题+xlsx说明均自洽；第一套/领导版 xlsx自洽但 docx 标题无"（第X套）"编号（源版固有格式）。
- 待确认差异：①模块B命名 第一套=模块B1.eprj2、二/三套=模块B.eprj2；②模块C压缩包 第三套=模块C.zip（其余=模块Cgerber.zip）；③第二套 模块D.eprj2 为第一套副本（非原生）；④模块C-01工艺卡参考答案三套 MD5 完全相同（共用模板）；⑤第一套样题docx 标题未带编号。
- MD5：样题docx三套各不相同（独立试卷）；模块C-02缺陷表、模块D检测表三套各不相同（独立缺陷布置）。
